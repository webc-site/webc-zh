# 国际化语言状态管理

## `langGet()` : 获取当前语言标识信息

### 返回值

当前语言标识数组 [lang_name, lang_id]

## `onLang` : 订阅语言变更事件，并在已设定语言时立即触发回调

### 参数

- func : 语言变更时的回调函数。参数为当前语言标识数组 name_id，无返回值。

### 返回值

用于取消订阅的卸载函数

## `langSet` : 设置当前语言并通知所有订阅者

### 参数

- name_id : 目标语言标识数组 [lang_name, lang_id]
