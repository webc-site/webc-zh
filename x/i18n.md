# 国际化语言状态管理

## `langGet()` : 获取当前设定的语言

## `onLang` : 订阅语言变更事件，并在订阅时如果已设定语言则立即触发回调

### 参数

- func : 语言变更时的回调函数。参数为当前语言(lang)，无返回值。

### 返回值

用于取消订阅的卸载函数，调用后将不再接收语言变更通知。

## `langSet` : 设置当前语言并通知所有订阅者

### 参数

- lang : 目标语言标识字符串
